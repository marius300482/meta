<div class="pagination">
    <? $chars = $this->paginationChars;
    foreach($chars as $char):
        if ($char['displayText'] == $this->char): ?>
            <span><?= $char['displayText'] ?></span>
        <? else: ?>
            <a href="?facet_prefix=<?= $char['displayText'] ?>"><?= $char['displayText'] ?></a>
        <? endif; ?>
    <? endforeach; ?>
</div>