<div class="pagination">
    <? $chars = $this->paginationChars;
    foreach($chars as $c):
        if ($c['displayText'] == $this->currentChar): ?>
            <span><?= $c['displayText'] ?></span>
        <? else: ?>
            <a href="?facet_prefix=<?= $c['displayText'] ?>"><?= $c['displayText'] ?></a>
        <? endif; ?>
    <? endforeach; ?>
</div>